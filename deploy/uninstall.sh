#!/usr/bin/env bash
set -euo pipefail
echo "Stop and disable mineshark services, then remove only the listed managed files after backup."
echo "This script intentionally performs no automatic deletion. See docs/real_sensor_deployment.md."
